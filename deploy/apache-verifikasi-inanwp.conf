# Template Apache — tambahkan ke vhost portal websitepsimkg
# SEBELUM catch-all ProxyPass / ke 127.0.0.1:3001
#
# Setelah edit:
#   sudo apache2ctl configtest
#   sudo systemctl reload apache2

# === Verifikasi InaNWP (sub-app PSIMKG) ===

# API backend — rule lebih spesifik, HARUS di atas rule frontend
ProxyPass        /verifikasi-inanwp/api http://127.0.0.1:8013/
ProxyPassReverse /verifikasi-inanwp/api http://127.0.0.1:8013/

# Frontend static (Vite build dengan base /verifikasi-inanwp/)
ProxyPass        /verifikasi-inanwp     http://127.0.0.1:3013/verifikasi-inanwp
ProxyPassReverse /verifikasi-inanwp     http://127.0.0.1:3013/verifikasi-inanwp

# === end Verifikasi InaNWP ===

# (existing portal catch-all — JANGAN UBAH)
# ProxyPass        / http://127.0.0.1:3001/
# ProxyPassReverse / http://127.0.0.1:3001/
